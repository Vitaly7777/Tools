print("Hello World")
...
```

Распаковщик извлекает только секции с `ФАЙЛ: путь`, игнорируя служебную информацию.

## ⚠️ Важные замечания

### Безопасность

  * Распаковщик не позволяет писать файлы за пределами текущей директории (проверяет `..` в путях)
  * Запрашивает подтверждение перед перезаписью существующих файлов
  * Не выполняет код, только записывает файлы

### Кодировки

  * Все файлы читаются и пишутся в UTF-8
  * При чтении бандла пробуются разные кодировки (UTF-8, CP1251, и т.д.)

### Ограничения

  * Максимальный размер файла можно ограничить параметром `--max-size`
  * Слишком большие файлы будут пропущены

### Совместимость

  * Утилиты работают на Python 3.6+
  * Не требуют сторонних библиотек (только стандартная библиотека)

---

## 🐍 Требования

  * Python 3.6 или выше
  * Только стандартная библиотека (никаких дополнительных пакетов)

---

## 📝 Рекомендации

  1. **Для отправки по email**: используйте `bundle.py` перед отправкой, `unbundle.py` после получения

  2. **Для бэкапов**: добавьте дату в имя файла: `bundle.py -o backup_$(date +%Y%m%d).txt`

  3. **Для больших проектов**: используйте `--exclude` для исключения ненужных директорий

  4. **Перед распаковкой**: всегда используйте `--dry-run` или `--list-files` для проверки содержимого

---

## 🔧 Устранение проблем

**Проблема:** `bundle.py` не видит мои файлы

**Решение:** Проверьте, что файлы имеют расширение `.py` или `.md` (если включены)


**Проблема:** При распаковке файлы не создаются

**Решение:** Проверьте права на запись в текущей директории


**Проблема:** Ошибка кодировки при чтении бандла

**Решение:** Убедитесь, что бандл создан той же версией утилиты


**Проблема:** Распаковщик пропускает файлы

**Решение:** Используйте `--no-skip` для восстановления всех секций

---

## 📁 Исходный код

Полные исходники доступны в файлах:

  * `bundle.py` - упаковщик
  * `unbundle.py` - распаковщик
